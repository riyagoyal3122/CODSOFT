# Password Generator

import random
import string
Password_length = int(input("Enter the length of password you want to create:"))
if Password_length >= 8 :
    Password = [random.choice(string.ascii_lowercase), random.choice(string.ascii_uppercase), random.choice(string.digits), random.choice("!@#$%^&*")]
    Password = Password + random.choice(srting.ascii_lowercase + 
    print("PASSWORD SHOULD BE MINIMUN OF 8 CHARACTERS")

